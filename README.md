# Bitrix24 MySQL API

REST API для доступа к данным Bitrix24 из MySQL базы данных на TimeWeb.

## 🚀 Деплой на Vercel через Manus

### Шаг 1: Подготовка

1. Скачай этот проект
2. Открой в Manus

### Шаг 2: Настройка переменных окружения

В Vercel добавь следующие переменные:

```
DB_HOST=5.129.206.213
DB_PORT=3306
DB_USER=gen_user
DB_PASSWORD=t6nfb4TEUhf}r{
DB_NAME=default_db
```

### Шаг 3: Деплой

Manus автоматически задеплоит на Vercel через интеграцию.

## 📡 API Endpoints

После деплоя API будет доступен по адресу: `https://your-project.vercel.app`

### Основные endpoints:

- `GET /` - Информация об API
- `GET /health` - Проверка подключения к БД
- `GET /tables` - Список всех таблиц
- `GET /table/:tableName` - Данные из таблицы
- `GET /table/:tableName/structure` - Структура таблицы
- `POST /query` - Выполнить SELECT запрос

### Специфичные endpoints:

- `GET /deals?limit=100` - Сделки
- `GET /companies?limit=100` - Компании  
- `GET /contacts?limit=100` - Контакты
- `GET /leads?limit=100` - Лиды

## 🔒 Безопасность

- API принимает только SELECT запросы
- Нет возможности изменять/удалять данные
- Credentials хранятся в переменных окружения

## 📊 Примеры использования

### Получить список таблиц
```bash
curl https://your-project.vercel.app/tables
```

### Получить сделки
```bash
curl https://your-project.vercel.app/deals?limit=10
```

### Выполнить кастомный запрос
```bash
curl -X POST https://your-project.vercel.app/query \
  -H "Content-Type: application/json" \
  -d '{"query": "SELECT * FROM deal WHERE closed = 1 LIMIT 10"}'
```
